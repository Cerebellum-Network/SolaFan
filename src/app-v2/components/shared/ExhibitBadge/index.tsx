import {memo} from 'react';

import {AuctionLiveBadge} from '../../primitives/Badge/AuctionLiveBadge';

export const ExhibitBadge = memo(() => {
  // TODO - add logic for render exhibit badge
  return <AuctionLiveBadge />;
});
